#!/bin/sh

# config
userresources=$HOME/.Xresources
usermodmap=$HOME/.Xmodmap

# set session-wide constants
export EDITOR="nano"
export DRI_PRIME=1
export GTK_MODULES=rgba

# append path
[ -d ~/bin/ ] && export PATH=~/bin:$PATH
[ -d ~/bin/internet-apps ] && export PATH=~/bin/internet-apps:$PATH

# merge in defaults and keymaps
[ -f "$userresources" ] && xrdb -load "$userresources"
[ -f "$usermodmap" ] && xmodmap "$usermodmap"

# start some nice programs
if [ -d /etc/X11/xinit/xinitrc.d ] ; then
 for f in /etc/X11/xinit/xinitrc.d/?*.sh ; do
  [ -x "$f" ] && . "$f"
 done
 unset f
fi

# Set Energy Features off
xset -dpms
# Set screensaver off
xset s off
# Set bell off
xset b off

# Key Options
xbindkeys
# Make the Print/Druck key the Super_R key
setxkbmap -option "altwin:prtsc_rwin"
# Deactivate capslock
setxkbmap -option "caps:none"

#export KDEWM=spectrwm
#exec startkde
#exec gnome-session
#exec sway
exec  awesome
